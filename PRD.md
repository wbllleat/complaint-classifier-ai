# 投诉归档意见AI分类工具 —— 产品需求文档（PRD）

> 版本：v1.0 | 日期：2026-06-09 | 状态：MVP 规划中

---

## 1. 产品概述

### 1.1 产品定位

**投诉归档意见AI分类工具** 是一个本地运行的桌面脚本工具，利用大语言模型（LLM）对运营商投诉工单中的归档意见字段进行语义分析与自动分类，替代人工逐条阅读归档意见的分类工作，实现批量、高效、一致的工单后处理。

本工具定位为 **分析师/投诉处理人员的效率工具**，而非面向终端客户的SaaS产品。核心交互方式为：选择一个Excel文件 → 配置分类规则 → 运行AI分类 → 获得带分类结果和汇总统计的Excel输出。

### 1.2 目标用户

| 用户角色 | 核心场景 | 痛点 |
|----------|----------|------|
| **投诉分析员** | 每月对万级投诉工单进行归档意见分类统计，生成月报 | 人工阅读18000+条归档意见耗时2-3天，分类标准因人而异 |
| **质检/运营主管** | 定期抽检归档意见质量，识别处理不当的工单 | 无法快速定位解释不到位/未联系客户等问题的工单 |
| **数据分析师** | 基于分类结果做趋势分析、根因分析 | 缺乏结构化的分类标签，现有数据无法直接用于BI分析 |

### 1.3 核心价值

`
输入：投诉工单Excel（含归档意见列） + 分类规则配置
输出：原Excel + 分类标签列 + 分类汇总统计表
耗时：18000行 → 约10-15分钟（vs 人工2-3天）
成本：约 ¥2-5/次（API费用）
`

### 1.4 核心设计原则

- **本地优先**：数据不出用户机器，原始Excel敏感信息不上传第三方SaaS
- **人机协同**：AI分类结果 = 建议，用户可人工复核和修正
- **确定性输出**：相同输入 + 相同Prompt → 一致的分类结果（通过JSON Schema约束）
- **可演进**：Prompt和分类体系作为可配置项，不硬编码入脚本

---

## 2. 功能列表

### 2.1 数据输入模块

| 功能编号 | 功能名称 | 描述 |
|----------|----------|------|
| F-01 | Excel文件导入 | 支持 .xlsx 格式，自动检测工作表，预览表头和前N行数据 |
| F-02 | 列映射配置 | 自动识别归档意见列（从表头匹配），支持用户手动指定目标列 |
| F-03 | 列结构校验 | 检查表头是否包含预期列名，对缺失/多余列给出提示 |
| F-04 | 空值/异常数据预览 | 统计归档意见为空的行数及占比，提供预览入口 |

### 2.2 分类配置模块

| 功能编号 | 功能名称 | 描述 |
|----------|----------|------|
| F-05 | 分类体系定义 | 用户定义分类标签树（如：一级分类→二级分类），支持JSON/YAML配置 |
| F-06 | 分类Prompt编辑 | 可视化编辑发送给LLM的分类Prompt模板，内置默认模板 |
| F-07 | 分类规则预览 | 用小样本（5-10条）测试当前Prompt和分类体系的输出效果 |
| F-08 | 分类维度预设 | 内置电信投诉常用分类模板（按问题类型、按处理方式） |

### 2.3 AI分类执行模块

| 功能编号 | 功能名称 | 描述 |
|----------|----------|------|
| F-09 | 批量分类执行 | 逐行/分批调用LLM API进行语义分类，每行输出结构化分类标签 |
| F-10 | 多级分类支持 | 支持一级+二级分类，以结构化JSON输出 |
| F-11 | 进度可视化 | 实时显示处理进度（已完成/总数、预计剩余时间、当前处理速度） |
| F-12 | 断点续跑 | 支持中断后从上次进度继续，已分类行不重复处理 |
| F-13 | 速率控制 | 内置API速率限制和429重试机制（指数退避），可配置并发数 |
| F-14 | 结果缓存 | 基于归档意见内容哈希的缓存，相同文本不重复调用API |

### 2.4 结果输出模块

| 功能编号 | 功能名称 | 描述 |
|----------|----------|------|
| F-15 | 分类标签回写 | 在原Excel中新增分类列（一级分类、二级分类），保留所有原始数据 |
| F-16 | 分类汇总报表 | 自动生成分类统计表（各分类数量、占比、饼图/柱状图数据） |
| F-17 | 交叉分析 | 支持按受理渠道x分类、客户星级x分类、城市x分类等维度交叉统计 |
| F-18 | 置信度标记 | 对AI低置信度分类结果标注待复核，方便人工抽检 |
| F-19 | 异常数据导出 | 单独导出AI无法分类、低置信度、归档意见为空的工单列表 |

### 2.5 人工复核模块

| 功能编号 | 功能名称 | 描述 |
|----------|----------|------|
| F-20 | 分类结果预览 | 以表格形式展示分类结果，支持按分类标签筛选 |
| F-21 | 人工修正 | 点击某行可手动修改分类标签，修正记录被标记 |
| F-22 | 修正反馈 | 将人工修正的样本保存为Few-Shot示例，用于优化后续批次Prompt |

---

## 3. 功能优先级

### 3.1 MVP（v0.1.0）—— 跑通核心链路

| 优先级 | 功能 | 理由 |
|--------|------|------|
| **P0** | F-01 Excel导入 + F-02 列映射 | 无输入则无输出 |
| **P0** | F-05 分类体系定义（初始硬编码3-5个分类） | 先验证分类可行性 |
| **P0** | F-09 批量分类执行（串行，单线程） | 核心功能 |
| **P0** | F-15 分类标签回写Excel | 核心交付物 |
| **P0** | F-16 基础汇总统计 | 用户最终要的是统计结果 |
| **P0** | F-13 速率控制 + 重试（429处理） | 否则大批量必然失败 |
| **P0** | F-14 结果缓存 | 避免重复调用API，节省成本 |

MVP 交付形态：**命令行Python脚本**，通过 config.yaml 配置。

### 3.2 v0.2.0 —— 提升可靠性和易用性

| 优先级 | 功能 | 理由 |
|--------|------|------|
| **P1** | F-04 空值/异常数据检测 | 提升数据质量感知 |
| **P1** | F-06 Prompt模板化（外部YAML文件） | 无需改代码即可调Prompt |
| **P1** | F-07 小样本测试模式（dry-run） | 调Prompt必备 |
| **P1** | F-11 进度可视化（终端进度条） | 18000行跑十几分钟，无进度焦虑 |
| **P1** | F-12 断点续跑 | 中断后不从头开始 |
| **P1** | F-18 低置信度标记 | 人工复核入口 |
| **P1** | F-17 基础交叉统计 | 按渠道/星级/城市维度 |

### 3.3 v0.3.0 —— 优化体验

| 优先级 | 功能 | 理由 |
|--------|------|------|
| **P2** | F-10 多级分类完善 | 从二级扩展到三级 |
| **P2** | F-08 分类维度预设模板库 | 降低新用户配置成本 |
| **P2** | F-19 异常数据单独导出 | 辅助质检 |
| **P2** | F-20 分类结果Web预览 | 从CLI升级为轻量Web UI |
| **P2** | F-21 人工修正交互 | Web界面内点击修正 |
| **P2** | F-22 修正反馈到Prompt迭代 | 持续优化分类质量 |

### 3.4 v1.0.0 —— 产品化

| 优先级 | 功能 | 理由 |
|--------|------|------|
| **P3** | F-03 列结构智能校验 | 减少用户误操作 |
| **P3** | 本地模型支持（Ollama对接） | 完全离线，零API费用 |
| **P3** | 多文件批量处理 | 一次处理多个Excel |
| **P3** | 历史任务管理 | 查看/对比历史分类结果 |
| **P3** | Web全功能界面 | 正式产品化 |

---

## 4. 界面设计要求

### 4.1 MVP阶段（CLI）

MVP采用**命令行交互**，设计原则：最小化用户输入、清晰的进度反馈、明确的输出路径。

    > python -m complaint_classifier run input.xlsx
    
    [读取文件] 广义投诉流量202605.xlsx
    [检测到工作表] Sheet1
    [表头] 工单号 | 投诉主题 | ... | 归档意见 | ... | 业务地市
    [归档意见列] H (第8列)
    [数据统计] 行数: 18992 | 有效归档意见: 18633 | 空值: 359
    
    [当前分类体系 (config.yaml)]
      一级分类: 费用争议 | 服务质量 | 业务办理 | 网络质量 | 其他
      二级分类: (按一级分类各有子项)
    
    [LLM配置] gpt-4o-mini | 并发: 3 | 缓存: 启用
    
    [开始分类] (预计 12 分钟)
    
    [########################################                    ] 65% (12112/18633)
    速率: 28条/秒 | 剩余: 4分钟
    
    [分类完成!]
      总耗时: 11分23秒
      处理条数: 18633
      缓存命中: 1521 (8.2%)
      API调用: 17112
      API费用: 3.42元
    
    [输出文件]
      结果: output/广义投诉流量202605_classified.xlsx
      汇总: output/广义投诉流量202605_summary.xlsx

### 4.2 v0.3.0+（Web UI）

Web界面采用**单页应用（SPA）**，设计风格参考数据分析工具（如Metabase轻量版）。

页面导航：[任务列表] [新建任务] [分类配置] [历史记录]

新建分类任务页面包含：
- 上传区域：支持拖拽或点击选择 .xlsx 文件，显示文件名和行数
- 分类配置：预设模板下拉、一级/二级分类可展开编辑
- 高级选项：模型选择、缓存开关、断点续跑
- 操作按钮：小样本测试(5条)、开始分类

分类进度区域：进度条 + 百分比 + 预估剩余时间

分类结果预览区域：统计表格（分类名称、数量、占比）+ 导出按钮

### 4.3 交互细节要求

- **拖拽上传**：支持拖拽 .xlsx 文件到上传区
- **实时进度**：进度条 + 百分比 + 预估剩余时间
- **就地编辑**：分类标签支持双击直接修改
- **颜色标记**：低置信度行黄色底色，人工修正行蓝色底色
- **响应式**：适配1920x1080及以上分辨率

---

## 5. 技术栈建议

### 5.1 MVP（CLI脚本）

| 层级 | 技术选择 | 理由 |
|------|----------|------|
| **语言** | Python 3.11+ | 数据处理生态最成熟，openai SDK官方支持 |
| **Excel读写** | openpyxl（读写.xlsx） | 纯Python，保留格式/样式，无需Excel安装 |
| **LLM调用** | DeepSeek Python SDK | 国产原生 |
| **配置管理** | pyyaml + config.yaml | 人类可读，适合Prompt存储 |
| **进度条** | tqdm | 终端进度显示的事实标准 |
| **缓存** | diskcache 或 SQLite | 基于归档意见哈希的去重缓存 |
| **包管理** | pyproject.toml（PEP 621） | 现代Python项目标准 |
| **类型检查** | mypy（strict mode） | 遵循严格类型原则 |

### 5.2 v0.3.0+（Web UI）

| 层级 | 技术选择 | 理由 |
|------|----------|------|
| **后端框架** | FastAPI | 高性能异步Python框架，原生支持后台任务 |
| **前端框架** | React 18 + TypeScript | 交互复杂度的最佳平衡 |
| **UI组件** | Ant Design | 成熟的React组件库，Table组件功能强大 |
| **图表** | ECharts（通过echarts-for-react） | 饼图/柱状图/交叉分析可视化 |
| **状态管理** | Zustand | 轻量，适合中等复杂度应用 |
| **API通信** | Server-Sent Events（SSE） | 实时推送分类进度 |
| **打包** | electron-builder（可选） | 如需独立桌面应用 |
| **本地存储** | SQLite | 任务历史、缓存、配置持久化 |

### 5.3 模型选择建议

| 模型 | 单条成本（~300字） | 准确率 | 适用场景 |
|------|--------------------|--------|----------|
| gpt-4o-mini | 约0.001元 | 4星 | **推荐：MVP首选**，性价比最优 |
| deepseek-chat | 约0.0005元 | 4星 | 备选：成本更低，中文能力强 |
| qwen-turbo | 约0.0003元 | 3星 | 阿里云生态用户首选 |
| claude-3-haiku | 约0.002元 | 4星 | 需要更谨慎推理时 |
| 本地 qwen2.5:7b | 免费 | 3星 | v1.0离线场景，需硬件支持 |

### 5.4 项目结构建议

    complaint-classifier/
    ├── pyproject.toml              # 项目元数据和依赖
    ├── config.yaml                 # 默认配置文件（分类体系、Prompt模板）
    ├── README.md
    ├── PRD.md
    ├── src/
    │   └── complaint_classifier/
    │       ├── __init__.py
    │       ├── __main__.py         # CLI入口
    │       ├── config.py           # 配置加载与校验
    │       ├── reader.py           # Excel读取与解析
    │       ├── classifier.py       # LLM分类核心逻辑
    │       ├── cache.py            # 结果缓存（去重）
    │       ├── writer.py           # 结果回写Excel
    │       ├── summary.py          # 汇总统计
    │       ├── progress.py         # 进度跟踪
    │       └── types.py            # 类型定义（TypedDict/dataclass）
    ├── tests/
    │   ├── test_reader.py
    │   ├── test_classifier.py
    │   └── test_writer.py
    ├── prompts/                    # Prompt模板文件
    │   └── default_classify.yaml
    └── output/                     # 输出目录

---

## 6. 非功能性需求

### 6.1 性能

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| **单条分类延迟** | < 2秒（P95） | 包含网络往返 + LLM推理 |
| **18000行总耗时** | < 20分钟（串行）/ < 8分钟（并发3） | 端到端计时 |
| **缓存命中延迟** | < 10ms | 本地SQLite查询 |
| **Excel读取（19000行）** | < 3秒 | 计时 |
| **Excel写入（19000行+新列）** | < 5秒 | 计时 |
| **内存占用峰值** | < 500MB | 处理过程中监控 |
| **并发API连接数** | 可配置1-10 | 默认3 |

### 6.2 可靠性

| 指标 | 目标值 | 策略 |
|------|--------|------|
| **API调用成功率** | > 99.5% | 指数退避重试（最多3次），429自动等待 |
| **断点续跑** | 中断后数据零丢失 | 每处理N条自动保存检查点 |
| **数据完整性** | 输出行数 = 输入行数 | 逐行编号校验 |
| **分类一致性** | 相同输入→相同输出 | JSON Schema约束 + Few-Shot示例固定 |

### 6.3 安全性

| 要求 | 策略 |
|------|------|
| **数据不离开本地** | 除LLM API调用外，不上传任何数据到第三方服务 |
| **API Key安全** | 支持环境变量 OPENAI_API_KEY，不硬编码在配置文件中 |
| **敏感字段脱敏** | 发送给LLM前自动脱敏手机号（可选，config.yaml配置） |
| **输出文件权限** | 输出文件继承输入文件所在目录权限 |

### 6.4 可维护性

| 要求 | 策略 |
|------|------|
| **Prompt可迭代** | Prompt存储在独立YAML文件中，修改无需改代码 |
| **分类体系可配置** | 分类标签树定义在配置文件中 |
| **日志可追溯** | 每次运行生成带时间戳的日志文件 |
| **分类结果可审计** | 每条分类结果记录模型版本、Prompt版本、时间戳 |

### 6.5 可扩展性

| 要求 | 策略 |
|------|------|
| **多模型支持** | 通过统一接口抽象LLM调用，支持OpenAI/Claude/Ollama无缝切换 |
| **多文件格式** | 设计Reader接口，后续扩展支持CSV/数据库源 |
| **自定义分类维度** | 支持问题归类、处理方式、客户情绪、是否解决等多维同时分类 |

### 6.6 AI上下文失忆问题解决方案

LLM批量分类中的核心挑战：分类标准在多轮会话中漂移、模型忘记初始分类规则。

**根本方案：每条分类请求都是独立、自包含的**

| 措施 | 说明 |
|------|------|
| **单轮独立调用** | 每条归档意见独立发送给LLM，不依赖对话历史 |
| **System Prompt固化** | 分类规则和示例固定在System Prompt中，每次调用都重新加载 |
| **JSON Schema约束** | 使用结构化输出（JSON mode），确保分类标签始终格式一致 |
| **Few-Shot锚定** | 每批次携带固定的3-5个标准分类示例，防止标准漂移 |
| **一致性抽样** | 每N条后自动用标准样本重测，检测分类标准是否漂移 |

---

## 附录

### A. 分类体系草案（电信投诉场景）

    classification:
      费用争议:
        - 流量超套扣费
        - 增值业务扣费
        - 套餐费用不符
        - 账单异常
        - 主副卡费用
      服务质量:
        - 解释不到位
        - 未及时联系客户
        - 处理方案不合理
        - 服务态度问题
      业务办理:
        - 套餐变更争议
        - 合约/协议争议
        - 办理流程问题
        - 营销宣传争议
      网络质量:
        - 信号覆盖问题
        - 网速不达标
        - 断网/掉线
      其他:
        - 无法分类

### B. 归档意见处理结果分类（交叉分类维度）

    resolution_type:
      - 直接解释（未退费）
      - 解释+退费
      - 解释+补偿（非退费）
      - 联系不上客户（未处理）
      - 升级处理/转交
      - 客户撤销/已解决
    
    resolution_outcome:
      - 客户接受
      - 客户未接听/未联系上
      - 客户不接受
      - 待跟进
      - 未知
